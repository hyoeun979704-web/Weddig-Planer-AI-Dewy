// Thin re-export — VendorDetailPage detects place.category and dispatches
// the right per-category extras block via PlaceDetailLayout. The dedicated
// /appliances/:id route still works for back-compat and direct linking.
export { default } from "./VendorDetailPage";
