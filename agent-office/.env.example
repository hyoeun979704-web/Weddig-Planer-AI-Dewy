# Dewy 에이전트 오피스 — 환경변수. 실제 키는 .env 로 복사해 채우고 커밋 금지.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx

# (선택) 앱 어드민 승인 큐 연동 — 있으면 산출물을 Supabase agent_outputs 로 업로드(폰 승인).
# service_role 키는 RLS 우회 — 로컬에만, 깃/클라 금지.
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...service_role...
